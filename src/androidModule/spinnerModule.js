import {ViewPropTypes, requireNativeComponent} from 'react-native';

const settings = {
     name: 'SpinnerModule',
     propTypes: {
        ViewPropTypes,
     },
};

export default requireNativeComponent('SpinnerModule', settings);